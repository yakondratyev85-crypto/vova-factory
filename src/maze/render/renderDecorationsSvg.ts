import type { MazeSettings } from '../core/types';
import type { WorksheetTheme } from '../themes/themeTypes';
export function renderDecorationsSvg(width: number, height: number, settings: MazeSettings, theme: WorksheetTheme): string { if (settings.visualMode === 'minimal') return ''; const outline = settings.visualMode === 'coloring'; const fill = outline ? 'none' : theme.soft; const stroke = outline ? '#111' : theme.accent; const items = [[width*.12,height*.18,theme.icons.decor[0]],[width*.88,height*.2,theme.icons.decor[1]],[width*.13,height*.86,theme.icons.decor[2]],[width*.86,height*.82,theme.icons.decor[3]]]; return `<g opacity="${outline ? .85 : .72}">${items.map(([x,y,t],i)=>`<circle cx="${x}" cy="${y}" r="${18+i*3}" fill="${fill}" stroke="${stroke}" stroke-width="2"/><text x="${x}" y="${Number(y)+5}" text-anchor="middle" font-size="20" fill="${stroke}">${t}</text>`).join('')}</g>`; }
