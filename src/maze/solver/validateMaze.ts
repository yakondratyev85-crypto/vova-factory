import type { MazeModel, ValidationResult } from '../core/types';
export function validateMaze(model: MazeModel, lineWidth: number, pathWidth: number): ValidationResult { const messages: string[] = []; if (!model.solution.length) messages.push('Нет пути от старта до финиша.'); if (lineWidth < 1.5) messages.push('Линии слишком тонкие для печати.'); if (pathWidth < 14) messages.push('Проходы могут быть слишком узкими.'); if (model.start.x === model.finish.x && model.start.y === model.finish.y) messages.push('Старт и финиш совпадают.'); return { valid: messages.length === 0, messages: messages.length ? messages : ['Лабиринт валиден: путь найден, старт и финиш открыты.'] }; }
