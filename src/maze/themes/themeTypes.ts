import type { MazeTheme, VisualMode } from '../core/types';
export interface WorksheetTheme { id: MazeTheme; name: string; background: string; panel: string; wall: string; accent: string; accent2: string; soft: string; text: string; solution: string; icons: { start: string; finish: string; decor: string[] }; title: string; instruction: string; }
export function applyVisualMode(theme: WorksheetTheme, mode: VisualMode): WorksheetTheme { if (mode === 'coloring') return { ...theme, background: '#ffffff', panel: '#ffffff', wall: '#111111', accent: '#111111', accent2: '#111111', soft: '#ffffff', text: '#111111', solution: '#e11d48' }; if (mode === 'premium') return { ...theme, background: '#fbfaf7', panel: '#ffffff', soft: '#f4efe7', wall: '#252525' }; if (mode === 'minimal') return { ...theme, background: '#ffffff', panel: '#ffffff', accent: '#111827', accent2: '#6b7280', soft: '#ffffff', text: '#111827' }; return theme; }
